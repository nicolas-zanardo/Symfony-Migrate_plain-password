{% extends 'base.html.twig' %}

{% block title %}Sign up{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{  form_rest(form)}}
    <button type="submit">Ok</button>
    {{ form_end(form) }}
{% endblock %}